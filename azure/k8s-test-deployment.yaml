apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdex-app-test
  labels:
    app: pdex
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pdex
  template:
    metadata:
      labels:
        app: pdex
    spec:
      containers:
      - name: pdex
        image: acrpdexdev.azurecr.io/pdex:latest-test
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: pdex-secrets-test

---
apiVersion: v1
kind: Service
metadata:
  name: pdex-service-test
spec:
  selector:
    app: pdex
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pdex-ingress-test
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: test.pdex.gov.bc.ca
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pdex-service-test
            port:
              number: 80
